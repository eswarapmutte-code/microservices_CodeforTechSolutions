spring:
  application:
    name: customer-service
 datasource:
   url: jdbc:mysql://localhost:3306/customer_master
   driverClassName: com.mysql.cj.jdbc.Driver
   username: root
   password: root
  jpa:
   hibernate:
    ddl-auto: none
    show-sql: true\
spring:
  cloud:
    config:
      server:
        git:
          uri: <YOUR_GIT_REPOSITORY_URL>   # e.g., https://github.com/your-username/config-repo.git
          default-label: main               # or 'master', depending on your repo's default branch
          search-paths: '{application}'     # Tells the server to look for configs in subdirectories named after the application
    
  
  
   

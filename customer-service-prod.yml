global:
 message: welcome to Customer Service prod Environment

server:
  port: 8083

message: Hello from Customer prod!!!

spring:
  application:
    name: customer-service
 datasource:
   url: jdbc:mysql://localhost:3306/customer_prod
   driverClassName: com.mysql.cj.jdbc.Driver
   username: root
   password: root
 jpa:
  hibernate:
   ddl-auto: update
  show-sql: true
